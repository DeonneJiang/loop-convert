#include <stdio.h>
#include <stdlib.h>

int b;

int c = 10;

int main(){


    char * p = malloc(sizeof(char)*100);
    int d;
    int g = 10;

    static int h = 10;

    1 && 1;
    if ( 1 && 1);

    
    free(p);
    return 0;
    
}